#
# SMTP settings
#
SMTP_SERVER_HOST=host.docker.internal
SMTP_SERVER_PORT=1025
SMTP_SERVER_USER=admin
SMTP_SERVER_PASSWORD=passw0rd
SMTP_SENDER_FROM=no-replay@brickwall.com
SMTP_TEMPLATES=template
SMTP_USE_TLS=false